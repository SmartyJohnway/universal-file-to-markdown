name: Dependency review

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deferred-dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deferred validation
        run: |
          echo "Dependency review requires pull_request context and is deferred to final release validation."
          exit 1
