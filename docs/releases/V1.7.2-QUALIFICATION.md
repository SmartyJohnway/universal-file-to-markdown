# v1.7.2 Release Qualification Evidence Report

Task ID: `V1.7.2-AI-REVIEW-CROSS-FORMAT-TRIGGER-CLOSURE-ENCODING-AMBIGUITY-CONTAINMENT-NATIVE-TEXT-MARKDOWN-ROUTES-RUNTIME-CAPABILITY-DISCLOSURE-OCR-ADVISORY-TARGETS-DOCUMENTATION-AND-RELEASE-QUALIFICATION`

## Qualification Context

- **Target Version**: `1.7.2`
- **Release Type**: Patch Release (`release` and `agent-skill` profiles)
- **Qualified Source Commit SHA**: `b2218e9f2bc76885236d822745ee19c356bcf60c`
- **Source Repository**: `universal-file-to-markdown`
- **Branch**: `codex/v1.7.2-cross-format-ai-review-and-runtime-closure`
- **Published Stable Ancestor**: `v1.7.0` (published stable release on GitHub Release)
- **Unpublished Integration Milestone**: `v1.7.1` (merged to main, unpublished, superseded by v1.7.2)

## Environment & Dependency Details

- **Operating System**: Windows (Win32)
- **Python Version**: `3.11.15`
- **RapidOCR Version**: `1.4.4` (`rapidocr-onnxruntime>=1.4,<2`)
- **Tesseract Version**: `5.4.0.20240606`
- **Pandoc Version**: `not_installed` (optional route dependency, capability probe reports `passed_with_caveats`)
- **Pip Dependencies**: `requirements.txt` (qualified)

---

## Artifact Identification & SHA-256 Hashes

### 1. Release Package Profile
- **Filename**: `universal-file-to-markdown-1.7.2-release.zip`
- **SHA-256 Hash**: `020649C1868926580A7A3B35CDE103BBE51CA145FA1FF04CA3823D2C986444D8`
- **Sidecar File**: `universal-file-to-markdown-1.7.2-release.sha256`
- **Evidence Manifest**: `universal-file-to-markdown-1.7.2-release.manifest.json`
- **Top-Level Directory**: `universal-file-to-markdown-1.7.2/`
- **File Count**: 72
- **Deterministic Build Verification**: `PASSED` (two builds produce byte-identical SHA-256)
- **Profile Manifest Inclusions**: `package-manifests/release.json` and `package-manifests/agent-skill.json` included in package.
- **Rebuild Capability**: Extracted release package contains both profile manifests and rebuilds both profiles cleanly.

### 2. Agent Skill Package Profile
- **Filename**: `universal-file-to-markdown-1.7.2-skill.zip`
- **SHA-256 Hash**: `3A2FECDF906D38195680EA91CBD3C525C7D33E719C5D3CCC69D39E72430F2E96`
- **Sidecar File**: `universal-file-to-markdown-1.7.2-skill.sha256`
- **Top-Level Directory**: `universal-file-to-markdown/` (containing root `SKILL.md`)
- **File Count**: 45
- **Deterministic Build Verification**: `PASSED` (two builds produce byte-identical SHA-256)
- **Standalone Validation Contract**: `PASSED` (validates standalone inside isolated directory without repository manifests).

---

## Comprehensive Qualification Results Summary

| Check | Result | Details |
| :--- | :---: | :--- |
| **Qualified Source SHA** | `PASSED` | `b2218e9f2bc76885236d822745ee19c356bcf60c` (all runtime, package, test, schema, and status files committed prior to build). |
| **Version Consistency** | `PASSED` | `VERSION`, `SKILL.md`, `ai-review-request.schema.json`, `README`, `VERSIONING`, `PROJECT_STATUS`, `CHANGELOG` all synchronized to `1.7.2`. |
| **Full Pytest Suite** | `PASSED` | 224 / 224 passed (0 failures). |
| **Python Compile Check** | `PASSED` | `python -m compileall scripts tests` succeeded cleanly. |
| **Source Markdown Link Check** | `PASSED` | All local repository Markdown links resolved. |
| **Extracted Package Link Check** | `PASSED` | All Markdown links inside extracted `release` and `agent-skill` packages resolved. |
| **Release Package Validator** | `PASSED` | Allowlist, strict boundary, sidecar, manifest evidence, and path safety validated. |
| **Agent Skill Package Validator** | `PASSED` | Standalone structural validation in isolated directory passed. |
| **Extracted Release Rebuild Both Profiles**| `PASSED` | Extracted release package contains both profile manifests and rebuilds both profiles cleanly. |
| **Release Package Clean-Install** | `PASSED` | Fresh venv, 8 representative conversions (`passed`), 8 bundle validations (`passed`). |
| **Agent Skill Runtime Qualification**| `PASSED` | Extracted single skill dir qualification, 8 conversions & bundle validations (`passed`). |
| **Expanded 26-Case Unified Regression**| `PASSED` | 26 / 26 cases passed (24 format cases + 2 workflow cases), 0 failures, 0 mismatches, determinism `passed`. |
| **AI Review Regression Suite** | `PASSED` | 9 / 9 cases passed, 0 failures, canonical mutations `0`. |

---

## Expanded 26-Case Unified Regression Breakdown

### Case & Format Distribution
- **DOCX Cases** (4 passed): `docx-basic-formatting`, `docx-horizontal-merge`, `docx-vertical-merge`, `docx-merged-table-trigger`
- **XLSX Cases** (4 passed): `xlsx-merged-cells`, `xlsx-two-blocks`, `xlsx-formula`, `xlsx-merged-table-trigger`
- **PPTX Cases** (5 passed): `pptx-basic-text`, `pptx-table`, `pptx-picture`, `pptx-grouped-shape`, `pptx-merged-table-trigger`
- **PDF Cases** (4 passed): `pdf-digital-text`, `pdf-ocr-representative`, `pdf-ocr-table-accepted`, `pdf-ocr-table-rejected`
- **CSV Cases** (2 passed): `csv-big5`, `csv-gb18030-ambiguity`
- **JSON Cases** (1 passed): `json-unicode`
- **HTML Cases** (2 passed): `html-complex`, `html-merged-table-trigger`
- **TXT Cases** (1 passed): `txt-native-route`
- **MD Cases** (1 passed): `md-native-route`
- **Workflow Cases** (2 passed): `phase5-readable-projection-host-review`, `explicit-user-request-workflow`

### Aggregate Mismatch & Validation Counters
- `bundle_validation_failures`: **0**
- `unexpected_warning_cases`: **0**
- `chunk_limit_violations`: **0**
- `reference_errors`: **0**
- `rerun_mismatches`: **0**
- `baseline_mismatches`: **0**
- `workflow_failures`: **0**

---

## Technical Engineering Tracks Delivered & Verified

1. **Track A: Cross-Format AI Review Trigger Closure**:
   Unified `table_has_merged_geometry` helper checking `merged_cells_present`, `merged_cells`, `rowspan > 1`, `colspan > 1` across DOCX, XLSX, PPTX, and HTML tables.

2. **Track B: Controlled User-Requested AI Review**:
   Added `--force-user-request` and `--target-table` / `--target-element` CLI flags to `prepare_ai_review.py`. Strict validation enforces bundle validity, target existence in bundle, and prohibition of conflicting parameters. Adds `"trigger": {"type": "explicit_user_request", ...}` metadata.

3. **Track C: Concrete GB18030 Simplified Chinese Ambiguity Containment & Recovery**:
   Verified using real GB18030 Simplified Chinese content (`张伟\n北京市\nGB18030`). Without hint: detects `encoding_status: "ambiguous"`, sets `explicit_encoding_recommended: true`, and emits `ENCODING_AMBIGUOUS` warning. With `--encoding gb18030`: recovers exact original text cleanly.

4. **Track D: Native TXT and Markdown Routes**:
   Created native text converter (`convert_text.py`) for `.txt`, `.text`, `.log` and native Markdown converter (`convert_markdown.py`) for `.md`, `.markdown`. Evaluated natively in `router.py` without requiring Pandoc or MarkItDown.

5. **Track E: Capability Probe Runtime Disclosure**:
   Updated `capability_probe.py` to disclose route-aware optional dependencies (`markitdown`, `pandoc`, `tesseract`). Reports `missing_optional_dependencies`, `affected_routes`, and sets status to `passed_with_caveats` when optional tools are missing without failing preflight.

6. **Track F: OCR Advisory Review Targets**:
   Unverified or rejected OCR table structures generate `advisory` targets (`target_type: "advisory"`). `projection_write_allowed: false` and `canonical_mutation_allowed: false` are enforced; `validate_ai_review.py` rejects any AI replacement attempt.

7. **Track G: AI Review Documentation Fixes**:
   Fixed `references/ai_review_workflow.md` to reference `schemas/ai-review.schema.json`, correct readable projection CLI render command (`--review`), document user request CLI, and clarify package boundaries.

8. **Track H: Packaged Runner Hygiene**:
   `run_cross_format_regression.py` and `run_ai_review_regression.py` emit friendly stderr message and exit code `2` when `tests/` directory is missing. Excluded from Agent Skill package profile.

---

## External Platform Smoke Status

- **Claude Custom Skill Package Validation**: `PASSED` (Standalone single top-level directory structure `universal-file-to-markdown/` with root `SKILL.md` verified).
- **ChatGPT Work Skill Package Validation**: `PASSED` (Standalone single top-level directory structure `universal-file-to-markdown/` with root `SKILL.md` verified).

---

## Conclusion & Readiness

- **Status**: `READY_FOR_V1.7.2_REVIEW`
- **Remaining Release Blockers**: `none`
