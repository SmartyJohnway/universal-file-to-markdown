# v1.7.1 Release Checklist

This checklist tracks the release gating requirements for **v1.7.1** (`1.7.1` patch release).

---

## 1. Governance & Versioning Checklist

- [x] Canonical `VERSION` updated to `1.7.1`.
- [x] `SKILL.md` frontmatter and title synchronized to `1.7.1`.
- [x] `schemas/ai-review-request.schema.json` mirrored `skill_version` updated to `1.7.1`.
- [x] `README.md` & `README.zh-TW.md` updated to reference `1.7.1` stable and `1.7.0` previous stable.
- [x] `VERSIONING.md` updated with `1.7.1` stable version notes.
- [x] `docs/PROJECT_STATUS.md` updated with final qualification evidence.
- [x] `CHANGELOG.md` & `CHANGELOG.zh-TW.md` updated with `## [1.7.1]` sections.
- [x] `tests/test_version_consistency.py` passing (3/3).

---

## 2. Package Profiles & Tooling Checklist

- [x] Dual package profile manifests created in `package-manifests/release.json` and `package-manifests/agent-skill.json`.
- [x] `scripts/build_skill_package.py` updated with `--profile {release, agent-skill}` options.
- [x] `scripts/validate_skill_package.py` updated with `--profile {release, agent-skill}` options.
- [x] `scripts/qualify_agent_skill_package.py` added for standalone Agent Skill runtime qualification.
- [x] `references/ai_review_workflow.md` added and referenced in `SKILL.md`.

---

## 3. Verification & Qualification Checklist

- [x] Source Markdown links check: `python scripts/check_markdown_links.py .` -> `passed`.
- [x] Full Pytest suite: `python -m pytest tests/ -q` -> `209 passed`.
- [x] Script compilation check: `python -m compileall scripts tests -q` -> `passed`.
- [x] Release Package deterministic build -> SHA-256 identical across two builds.
- [x] Agent Skill Package deterministic build -> SHA-256 identical across two builds.
- [x] Release Package validation -> `passed`.
- [x] Agent Skill Package validation -> `passed`.
- [x] Release Package clean-install qualification -> `passed`.
- [x] Agent Skill runtime qualification -> `passed`.
- [x] Final-Head Unified 18-case Cross-Format Regression -> `18/18 passed` (`validation_status: passed`).

---

## 4. Platform Upload Smoke Checklist

- [ ] ChatGPT Skill Upload Smoke (Manual check upon upload).
- [ ] Claude Custom Skill Upload Smoke (Manual check upon upload).

---

## 5. Release Approval

- [x] All automated qualification checks passed.
- [x] Working tree clean.
- [x] Ready for PR review and merge (do not tag or release before PR merge).
