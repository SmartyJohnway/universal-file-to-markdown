# Project Status

Current stable target: `v1.6.0`

Data schema version: `1.0`

Governance model: maintainer-led

License: Apache-2.0

Release readiness requires:

- required dependency preflight passed;
- full tests passed;
- Python sources compiled;
- GitHub Actions green;
- release checklist completed;
- annotated version tag on the verified `main` commit.

See `RELEASE_CHECKLIST.md` and `RELEASING.md` for the authoritative release process.